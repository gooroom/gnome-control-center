diff --git a/panels/info-overview/cc-info-overview-panel.c b/panels/info-overview/cc-info-overview-panel.c
index a5c0b6b..843115b 100644
--- a/panels/info-overview/cc-info-overview-panel.c
+++ b/panels/info-overview/cc-info-overview-panel.c
@@ -103,6 +103,12 @@ G_DEFINE_AUTOPTR_CLEANUP_FUNC (VersionData, version_data_free);
 
 G_DEFINE_TYPE (CcInfoOverviewPanel, cc_info_overview_panel, CC_TYPE_PANEL)
 
+static const char *
+cc_info_overview_panel_get_help_uri (CcInfoOverviewPanel *panel)
+{
+  return "help:gnome-help/details-information";
+}
+
 static void
 version_start_element_handler (GMarkupParseContext      *ctx,
                                const char               *element_name,
@@ -852,6 +858,9 @@ static void
 cc_info_overview_panel_class_init (CcInfoOverviewPanelClass *klass)
 {
   GtkWidgetClass *widget_class = GTK_WIDGET_CLASS (klass);
+  CcPanelClass *panel_class = CC_PANEL_CLASS (klass);
+
+  panel_class->get_help_uri = cc_info_overview_panel_get_help_uri;
 
   gtk_widget_class_set_template_from_resource (widget_class, "/org/gnome/control-center/info-overview/cc-info-overview-panel.ui");
 
diff --git a/panels/location/cc-location-panel.c b/panels/location/cc-location-panel.c
index cca3346..c79b8d8 100644
--- a/panels/location/cc-location-panel.c
+++ b/panels/location/cc-location-panel.c
@@ -384,7 +384,7 @@ cc_location_panel_finalize (GObject *object)
 static const char *
 cc_location_panel_get_help_uri (CcPanel *panel)
 {
-  return "help:gnome-help/location";
+  return "help:gnome-help/privacy-location";
 }
 
 static void
diff --git a/panels/lock/cc-lock-panel.c b/panels/lock/cc-lock-panel.c
index 86800e8..084fbd4 100644
--- a/panels/lock/cc-lock-panel.c
+++ b/panels/lock/cc-lock-panel.c
@@ -50,6 +50,12 @@ struct _CcLockPanel
 
 CC_PANEL_REGISTER (CcLockPanel, cc_lock_panel)
 
+static const char *
+cc_lock_panel_get_help_uri (CcLockPanel *self)
+{
+  return "help:gnome-help/privacy-screen-lock";
+}
+
 static void
 on_lock_combo_changed_cb (GtkWidget   *widget,
                           CcLockPanel *self)
@@ -258,6 +264,9 @@ cc_lock_panel_class_init (CcLockPanelClass *klass)
 {
   GObjectClass *oclass = G_OBJECT_CLASS (klass);
   GtkWidgetClass *widget_class = GTK_WIDGET_CLASS (klass);
+  CcPanelClass *panel_class = CC_PANEL_CLASS (klass);
+
+  panel_class->get_help_uri = cc_lock_panel_get_help_uri;
 
   oclass->finalize = cc_lock_panel_finalize;
 
diff --git a/panels/removable-media/cc-removable-media-panel.c b/panels/removable-media/cc-removable-media-panel.c
index a891855..599d3e4 100644
--- a/panels/removable-media/cc-removable-media-panel.c
+++ b/panels/removable-media/cc-removable-media-panel.c
@@ -64,6 +64,12 @@ struct _CcRemovableMediaPanel
 
 G_DEFINE_TYPE (CcRemovableMediaPanel, cc_removable_media_panel, CC_TYPE_PANEL)
 
+static const char *
+cc_removable_media_panel_get_help_uri (CcRemovableMediaPanel *self)
+{
+  return "help:gnome-help/file-autorun";
+}
+
 static void
 ellipsize_cell_layout (GtkCellLayout *cell_layout)
 {
@@ -484,6 +490,9 @@ cc_removable_media_panel_class_init (CcRemovableMediaPanelClass *klass)
 {
   GObjectClass   *object_class = G_OBJECT_CLASS (klass);
   GtkWidgetClass *widget_class = GTK_WIDGET_CLASS (klass);
+  CcPanelClass   *panel_class = CC_PANEL_CLASS (klass);
+
+  panel_class->get_help_uri = cc_removable_media_panel_get_help_uri;
 
   object_class->finalize = cc_removable_media_panel_finalize;
   object_class->dispose = cc_removable_media_panel_dispose;
diff --git a/panels/thunderbolt/cc-bolt-panel.c b/panels/thunderbolt/cc-bolt-panel.c
index 9004c3c..ebed1ad 100644
--- a/panels/thunderbolt/cc-bolt-panel.c
+++ b/panels/thunderbolt/cc-bolt-panel.c
@@ -150,6 +150,12 @@ static void      on_permission_notify_cb (GPermission *permission,
 
 CC_PANEL_REGISTER (CcBoltPanel, cc_bolt_panel);
 
+static const char *
+cc_bolt_panel_get_help_uri (CcBoltPanel *self)
+{
+  return "help:gnome-help/thunderbolt";
+}
+
 static void
 bolt_client_ready (GObject      *source,
                    GAsyncResult *res,
@@ -924,6 +930,9 @@ cc_bolt_panel_class_init (CcBoltPanelClass *klass)
 {
   GObjectClass *object_class = G_OBJECT_CLASS (klass);
   GtkWidgetClass *widget_class = GTK_WIDGET_CLASS (klass);
+  CcPanelClass *panel_class = CC_PANEL_CLASS (klass);
+
+  panel_class->get_help_uri = cc_bolt_panel_get_help_uri;
 
   object_class->constructed = cc_bolt_panel_constructed;
   object_class->dispose = cc_bolt_panel_dispose;
diff --git a/panels/thunderbolt/gnome-thunderbolt-panel.desktop.in.in b/panels/thunderbolt/gnome-thunderbolt-panel.desktop.in.in
index f7f9e4b..12ba033 100644
--- a/panels/thunderbolt/gnome-thunderbolt-panel.desktop.in.in
+++ b/panels/thunderbolt/gnome-thunderbolt-panel.desktop.in.in
@@ -8,11 +8,11 @@ Terminal=false
 Type=Application
 NoDisplay=true
 StartupNotify=true
-Categories=GNOME;GTK;Settings;X-GNOME-Settings-Panel;HardwareSettings;X-GNOME-PrivacySettings;
+Categories=GNOME;GTK;Settings;X-GNOME-Settings-Panel;HardwareSettings;X-GNOME-DevicesSettings;
 OnlyShowIn=GNOME;Unity;
 X-GNOME-Bugzilla-Bugzilla=GNOME
 X-GNOME-Bugzilla-Product=gnome-control-center
 X-GNOME-Bugzilla-Component=thunderbolt
 X-GNOME-Bugzilla-Version=@VERSION@
 # Translators: those are keywords for the thunderbolt control-center panel. Do NOT translate or localize the semicolons! The list MUST also end with a semicolon!
-Keywords=Thunderbolt;privacy;
+Keywords=Thunderbolt;
diff --git a/panels/usage/cc-usage-panel.c b/panels/usage/cc-usage-panel.c
index d4198b0..1fce984 100644
--- a/panels/usage/cc-usage-panel.c
+++ b/panels/usage/cc-usage-panel.c
@@ -48,6 +48,12 @@ CC_PANEL_REGISTER (CcUsagePanel, cc_usage_panel)
 
 static gboolean run_warning (CcUsagePanel *panel, char *title, const gchar *prompt, const gchar *text, const gchar *button_title);
 
+static const char *
+cc_usage_panel_get_help_uri (CcUsagePanel *self)
+{
+  return "help:gnome-help/privacy-purge";
+}
+
 static void
 purge_after_combo_changed_cb (CcUsagePanel *self)
 {
@@ -347,6 +353,9 @@ cc_usage_panel_class_init (CcUsagePanelClass *klass)
 {
   GObjectClass *oclass = G_OBJECT_CLASS (klass);
   GtkWidgetClass *widget_class = GTK_WIDGET_CLASS (klass);
+  CcPanelClass *panel_class = CC_PANEL_CLASS (klass);
+
+  panel_class->get_help_uri = cc_usage_panel_get_help_uri;
 
   oclass->finalize = cc_usage_panel_finalize;
 
diff --git a/shell/cc-panel-list.c b/shell/cc-panel-list.c
index 58817e6..a3509b0 100644
--- a/shell/cc-panel-list.c
+++ b/shell/cc-panel-list.c
@@ -441,7 +441,6 @@ static const gchar * const panel_order[] = {
   "location",
   "camera",
   "microphone",
-  "thunderbolt",
   "usage",
   "lock",
   "diagnostics",
@@ -452,6 +451,7 @@ static const gchar * const panel_order[] = {
   "keyboard",
   "printers",
   "removable-media",
+  "thunderbolt",
   "wacom",
   "color",
 
