diff --git a/panels/user-accounts/cc-add-user-dialog.c b/panels/user-accounts/cc-add-user-dialog.c
index 031eaa7..8e361a0 100644
--- a/panels/user-accounts/cc-add-user-dialog.c
+++ b/panels/user-accounts/cc-add-user-dialog.c
@@ -91,6 +91,7 @@ struct _CcAddUserDialog {
         GtkPasswordEntry   *local_verify_entry;
         AdwActionRow       *local_verify_password_row;
         GtkImage           *local_verify_status_icon;
+        GtkSwitch          *home_directory_encrypt;
         AdwPreferencesPage *offline_page;
         AdwPreferencesGroup *password_group;
         GtkSpinner         *spinner;
@@ -234,24 +235,38 @@ local_create_user (CcAddUserDialog *self)
         const gchar *username;
         const gchar *name;
         gint account_type;
+        gboolean encrypt_home;
 
         begin_action (self);
 
         name = gtk_editable_get_text (GTK_EDITABLE (self->local_name_entry));
         username = gtk_combo_box_text_get_active_text (self->local_username_combo);
         account_type = gtk_switch_get_active (self->local_account_type_switch) ? ACT_USER_ACCOUNT_TYPE_ADMINISTRATOR : ACT_USER_ACCOUNT_TYPE_STANDARD;
+        encrypt_home = gtk_switch_get_active (self->home_directory_encrypt);
 
         debian_username = g_ascii_strdown (username, -1);
         g_debug ("Creating local user: %s", debian_username);
 
         manager = act_user_manager_get_default ();
-        act_user_manager_create_user_async (manager,
-                                            debian_username,
-                                            name,
-                                            account_type,
-                                            self->cancellable,
-                                            (GAsyncReadyCallback)create_user_done,
-                                            self);
+
+        if (encrypt_home) {
+            act_user_manager_create_user_encrypt_async (manager,
+                                                        debian_username,
+                                                        name,
+                                                        account_type,
+                                                        encrypt_home,
+                                                        self->cancellable,
+                                                        (GAsyncReadyCallback)create_user_done,
+                                                        self);
+        } else {
+            act_user_manager_create_user_async (manager,
+                                                debian_username,
+                                                name,
+                                                account_type,
+                                                self->cancellable,
+                                                (GAsyncReadyCallback)create_user_done,
+                                                self);
+        }
 }
 
 static gint
@@ -1674,6 +1689,7 @@ cc_add_user_dialog_class_init (CcAddUserDialogClass *klass)
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, local_verify_entry);
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, local_verify_password_row);
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, local_verify_status_icon);
+        gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, home_directory_encrypt);
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, offline_page);
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, password_group);
         gtk_widget_class_bind_template_child (widget_class, CcAddUserDialog, spinner);
diff --git a/panels/user-accounts/cc-add-user-dialog.ui b/panels/user-accounts/cc-add-user-dialog.ui
index afdc65f..d1e2947 100644
--- a/panels/user-accounts/cc-add-user-dialog.ui
+++ b/panels/user-accounts/cc-add-user-dialog.ui
@@ -138,6 +138,19 @@
                     </child>
                   </object>
                 </child>
+                <child>
+                  <object class="AdwActionRow">
+                    <property name="activatable-widget">home_directory_encrypt</property>
+                    <property name="title" translatable="yes">Home directory _Encryption</property>
+                    <property name="subtitle" translatable="yes">Encrypt all files in user's home directory with user's login password to protect sensitive data from people who can access to user's computer.</property>
+                    <property name="subtitle-lines">3</property>
+                    <child type="suffix">
+                      <object class="GtkSwitch" id="home_directory_encrypt">
+                        <property name="valign">center</property>
+                      </object>
+                    </child>
+                  </object>
+                </child>
               </object>
             </child>
             <child>
diff --git a/panels/user-accounts/cc-user-panel.ui b/panels/user-accounts/cc-user-panel.ui
index 3f0362b..c5d3632 100644
--- a/panels/user-accounts/cc-user-panel.ui
+++ b/panels/user-accounts/cc-user-panel.ui
@@ -225,6 +225,7 @@
                             <property name="title" translatable="yes">A_utomatic Login</property>
                             <property name="use_underline">True</property>
                             <property name="activatable_widget">autologin_switch</property>
+							<property name="visible">False</property>
                             <child>
                               <object class="GtkSwitch" id="autologin_switch">
                                 <property name="valign">center</property>
diff --git a/po/en.po b/po/en.po
index fe2d8bd..76d5ec0 100644
--- a/po/en.po
+++ b/po/en.po
@@ -3300,3 +3300,12 @@ msgstr ""
 msgid "Show side panel list that matches the User settings values."
 msgstr ""
 
+#: ../panels/user-accounts/cc-add-user-dialog.ui:144
+msgid "Home directory _Encryption"
+msgstr ""
+
+#: ../panels/user-accounts/cc-add-user-dialog.ui:145
+msgid ""
+"Encrypt all files in user's home directory with user's login password" 
+" to protect sensitive data from people who can access to user's computer."
+msgstr ""
diff --git a/po/en_GB.po b/po/en_GB.po
index b9d2266..dbb42a0 100644
--- a/po/en_GB.po
+++ b/po/en_GB.po
@@ -16249,3 +16249,15 @@ msgstr "Show settings list"
 #: ../shell/org.gnome.ControlCenter.gschema.xml:21
 msgid "Show side panel list that matches the User settings values."
 msgstr "Show side panel list that matches the User settings values."
+
+#: ../panels/user-accounts/cc-add-user-dialog.ui:144
+msgid "Home directory _Encryption"
+msgstr "Home directory _Encryption"
+
+#: ../panels/user-accounts/cc-add-user-dialog.ui:145
+msgid ""
+"Encrypt all files in user's home directory with user's login password" 
+" to protect sensitive data from people who can access to user's computer."
+msgstr ""
+"Encrypt all files in user's home directory with user's login password" 
+" to protect sensitive data from people who can access to user's computer."
diff --git a/po/ko.po b/po/ko.po
index f683692..694081b 100644
--- a/po/ko.po
+++ b/po/ko.po
@@ -9763,6 +9763,18 @@ msgstr "패널 목록 보여줌"
 msgid "Show side panel list that matches the User settings values."
 msgstr "사용자 설정 값과 일치하는 사이드 목록을 보여줍니다."
 
+#: ../panels/user-accounts/cc-add-user-dialog.ui:144
+msgid "Home directory _Encryption"
+msgstr "홈폴더 암호화(E)"
+
+#: ../panels/user-accounts/cc-add-user-dialog.ui:145
+msgid ""
+"Encrypt all files in user's home directory with user's login password" 
+" to protect sensitive data from people who can access to user's computer."
+msgstr ""
+"사용자의 홈폴더에 있는 모든 파일을 사용자의 로그인 암호로 암호화하여"
+" 사용자의 컴퓨터에 물리적으로 접근 가능한 사람으로부터 민감한 정보를 보호합니다."
+
 #~ msgid "Show _Notifications on Lock Screen"
 #~ msgstr "알림 표시 및 화면 잠그기(_N)"
 
